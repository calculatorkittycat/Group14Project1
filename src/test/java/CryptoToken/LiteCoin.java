package CryptoToken;

public class LiteCoin extends CryptoToken {
    public LiteCoin(double price, int quantity, double marketCap, double volume, double circulatingSupply, boolean isMineable) {
        super(price, quantity, marketCap, volume, circulatingSupply, isMineable);
    }
}
